export const UIConfig = {
  theme: 'light', // Options: 'light', 'dark'
  dateFormat: 'MM/DD/YYYY',
  timeFormat: 'HH:mm:ss',
  itemsPerPage: 10, // Default number of items per page for pagination
  supportedLanguages: ['en', 'es', 'fr'], // Supported UI languages
  defaultLanguage: 'en'
};
